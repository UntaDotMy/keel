{
  "_comment": "keel MCP server registration for Cursor. Cursor loads MCP config from ~/.cursor/mcp.json (global) or .cursor/mcp.json (project) — see https://cursor.com/docs/mcp. STDIO entries require type=stdio, command, and args. The installer merges the keel entry without clobbering sibling servers.",
  "mcpServers": {
    "keel": {
      "type": "stdio",
      "command": "keel",
      "args": ["mcp", "serve"]
    }
  }
}
