[package]
name = "keel-sqlite-vec"
version = "0.1.10-alpha.4"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Vendored sqlite-vec FFI bindings (DiskANN disabled) for keel"
build = "build.rs"

[lib]
name = "keel_sqlite_vec"
path = "src/lib.rs"

[build-dependencies]
cc = "1.0"
