  Native orchestration tooling covers runtime-preflight (probes claude-home, the workflow ledger directory, and git --version), resume-status (lists open workflow ledger entries), task (begin/progress/complete/list, backed by the JSON record store under ~/.claude/orchestration/tasks), and checkpoint (write/show under ~/.claude/orchestration/checkpoints).
  Phase 3 native memory tooling covers scope resolution, native SYSTEM_MAP refresh under the project-scoped global SYSTEM_MAP target, working-brief write/show/list, completion-gate check, and SQLite FTS5-backed recall over Markdown and JSON stores under ~/.claude/{memories,working-briefs}. The memory families agent-registry, research-cache, maintenance, loop-guard, agent-packets, entity, graph, retrieve, instincts, and the status/report surface are all implemented in the Rust runtime.
  The autonomous learning loop observes tool-call behavior (PostToolUse), distills repeated signatures into confidence-scored instincts at SessionEnd, decays and prunes instincts whose patterns age out, and evolves trusted instinct clusters into generated SKILL.md skills plus a paired subagent under ~/.claude/{skills,agents}/learned-*. Inspect or trigger it with `keel learn [status|dry-run|run]`. Generated artifacts are marked provenance=learned and are never confused with built-in skills; the loop never overwrites a manually refined generated skill or a hand-authored instinct. Disable with CLAUDE_SKILLS_LEARNING=off.
