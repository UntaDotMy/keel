  Native orchestration tooling currently covers two surfaces: runtime-preflight (probes claude-home, the workflow ledger directory, and git --version) and resume-status (lists open workflow ledger entries). Task begin/progress/complete and checkpoint return non-zero with a "not implemented" message; treat them as no-ops until a ledger writer lands.
  Phase 3 native memory tooling currently covers scope resolution, native SYSTEM_MAP refresh under the project-scoped global SYSTEM_MAP target, working-brief write/show/list, completion-gate check, and SQLite FTS5-backed recall over Markdown stores under ~/.claude/{memories,memoriesv2,working-briefs}. Agent-registry, research-cache, maintenance, loop-guard, agent-packets, and the memory report surface return non-zero with a "not implemented" message in the Rust runtime.
  MemoriesV2 is the writable global second-layer memory store under ~/.claude/memoriesv2/, and the native runtime currently keeps scope/system-map/working-brief/completion-gate/recall paths synchronized with that root. Retrieval packets, ledger append/show, graph add/list/query, and consolidate are not yet wired in the Rust runtime and return non-zero.
